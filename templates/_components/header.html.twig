<header>
	<nav class="navbar navbar-expand-lg bg-body-tertiary">
		<div class="container-fluid">
			<a class="navbar-brand" href="#">Marina's and Anais's Shop</a>
			<button class="navbar-toggler" type="button" data-bs-toggle="collapse" data-bs-target="#navbarNavDropdown" aria-controls="navbarNavDropdown" aria-expanded="false" aria-label="Toggle navigation">
				<span class="navbar-toggler-icon"></span>
			</button>
			<div class="collapse navbar-collapse" id="navbarNavDropdown">
				<ul class="navbar-nav">
					<li class="nav-item">
						<a class="nav-link active" aria-current="page" href="{{path('home_index')}}">Home</a>
					</li>
					<li class="nav-item">
						<a class="nav-link" href="{{path('shop_index')}}">Shop</a>
					</li>

					<li class="nav-item dropdown">
						<a class="nav-link dropdown-toggle" href="#" role="button" data-bs-toggle="dropdown" aria-expanded="false">
							Categories
						</a>
						<ul class="dropdown-menu">
							{# {% for category in categories %}
								<li>
								<a class="dropdown-item" href="{{path('shop_category',{id :category.id })}}">{{category.label}}</a>
							</li>
							{% endfor %} #}

							{# <li><a href="{{ path('') }}">Voir les categories</a></li> #}
							
							{# <li>
								<a class="dropdown-item" href="{{path('shop_category',{id : 2})}}">Tarte</a>
							</li>
							<li>
								<a class="dropdown-item" href="{{path('shop_category',{id : 3})}}">Gateaux</a>
							</li> #}
						</ul>
					</li>
					<li class="nav-item">
						{% if app.user %}
							<p class="nav-link">bienvenue ,
								{{app.user.email}}</p>
							<a class="nav-link" href="{{path('app_logout')}}">Se déconnecter</a>
						{% else %}
							<li class="nav-item">
								<a class="nav-link" href="{{path('app_login')}}">Se connecter</a>
							</li>
							<li class="nav-item">
								<a class="nav-link" href="{{path('app_register')}}">S'enregistrer</a>
							</li>
						{% endif %}
					</li>
					<li>
						<a class="nav-link" href="{{path('admin_category')}}">Administrateur</a>
					</li>
				</ul>


			</div>
		</div>
	</nav>

</header>
