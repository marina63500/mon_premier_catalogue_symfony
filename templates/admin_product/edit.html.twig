{% extends "base.html.twig" %}


{% block body %}

    {{ form_start(form) }}     

            {{ form_row(form.name) }}
            {{ form_row(form.image) }}    
            {{ form_row(form.price) }}    
            {{ form_row(form.categories) }}    

	        <button type="submit">Soumettre</button>

    {{form_end(form)}}
 {% endblock %}